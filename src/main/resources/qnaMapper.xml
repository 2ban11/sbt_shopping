<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sbt.shopping.jaepil.QnaMapper">
	<select id="getQna" resultType="com.sbt.shopping.jaepil.QnaDTO">
		select rownum as rn, q_title, q_content, q_date
		from (
		select * from
		qna where q_id = 'mk' order by q_date desc
		)
	</select>
</mapper>